<script>
    var Routes = {
        AllVideos : "{{ path("api.video.all") }}",
        CreateVideo : "{{ path("api.video.create") }}",

        AllPhotos : "{{ path('api.photo.all') }}",
        CreatePhoto : "{{ path("api.photo.create") }}",

        AllPlatforms : "{{ path('api.platform.all') }}",

        AllEvents : "{{ path('api.event.all') }}",

        AllAdmins : "{{ path('api.admin.all') }}"
    };
</script>